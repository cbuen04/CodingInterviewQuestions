using System;
using System.Collections.Generic;

namespace LinkedLists
{
    /// <summary>
    /// Main take away from linked lists,
    /// in order to remove any element in the list you must iterate k elements before removign
    /// first element can be removed in CONSTANT TIME
    /// </summary>
    public class LinkList<T>
    {
        public static List<int> removeDuplicates(LinkedList<T> LL)
        {
            Dictionary<T, int> ListElements = new Dictionary<T, int>();

            return null;
        }
    }
}
